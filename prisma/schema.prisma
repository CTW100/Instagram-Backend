// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
} // 이 데이터소스는 Prisma에게 데이터베이스의 주소(URL)와 종류(Provider)에 대해 알려주는 녀석임

generator client {
  provider = "prisma-client-js"
}

// Prisma Migrate 는 이 shcema.prisma 파일의 데이터 모델을 쓰고 설명할 수 있게 해줌. 여기에서 모든 Model들 설명 --> Prisma Migrate은 그 설명들을 DB에 적용해줌.
model Movie {
  id Int @id @default(autoincrement()) // @id : db에게 이게 id임을 전달하기 위함
  title String
  year Int
  genre String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
// 이 다음에 칠 명령어 npx prisma migrate dev // 여기서 migrate dev : 개발 환경에서 작업 중